import Image from "next/image";

export default function Home() {
    return (
        <main>
            <span className="flex font-clash text-[10.5vw] md:text-[11.5vw] font-bold text-black-400 mb-[-1rem] justify-center">
                enzobeystudio*
            </span>
        </main>
    );
}
